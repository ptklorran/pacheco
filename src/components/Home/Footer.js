import React from 'react'
import { withStyles } from '@material-ui/core/styles'
import {  } from '@material-ui/core'

const style = theme => ({

})

class Footer extends React.Component {
    state = {
        //dados
    }

    render() {
        return (
            <div style={{display: 'flex', alignItems: 'center', justifyContent: 'center', width: '100%', height: '400px', backgroundImage: 'linear-gradient(#111111 1%, #555 90%)' }}>
                oi
            </div>
        )
    }
}

export default withStyles(style)(Footer)